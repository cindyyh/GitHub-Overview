name: first workflow

on:
  workflow_dispatch:

jobs:
  firstjob:
    runs-on: ubuntu-latest
    steps:
      - name: secondJob script
        shell: bash
        run: echo this is the first job!
      - name: checkout the repository
        uses: actions/checkout@v3
      - name: greeting to a user with the hello world function
        id: greetings
        uses: actions/hello-world-javascript-action@main
        with:
          who-to-greet: 'User'
      - name: the greeting time
        run: |
          echo "The time of the greeting was at: ${{ steps.greetings.outputs.time }}"

  secondjob:
    runs-on: ubuntu-latest
    steps:
      - name: secondJob script
        shell: bash
        run: |
          echo this is the second job
          echo and this is a multi-line script
